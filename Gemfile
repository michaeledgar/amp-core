# This is an install-only file - it's not used by the application
source "http://rubygems.org"

gem 'amp-front'

group :development do
  gem 'jeweler'
  gem 'rspec', '< 2.0.0'
  gem 'yard'
  gem 'cucumber'
  gem 'haml'  # for templates
end
